# Obsidian Sample Plugin

This is a simple plugin for [Obsidian](https://obsidian.md)'s Canvas that allows remvoing files from a canvas and automatically delete them from the vault as well.

It adds the command `Canvas Remove And Delete Files: Invoke` and you can configure your own hotkey in the Obsidian keyboard shortcut settings.

## Manually installing the plugin

- Copy over `main.js` and `manifest.json` to your vault `VaultFolder/.obsidian/plugins/canvas-remove-and-delete-files/`.
